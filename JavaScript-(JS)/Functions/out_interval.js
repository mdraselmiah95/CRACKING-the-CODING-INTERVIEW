// setTimeOut and setInterval

function greeting(name) {
  console.log(`Welcome to the Programming world 🌎 =>${name}`);
}

// setTimeout(greeting, 2000);

setTimeout(() => {
  console.log("Welcome to the Programming world 🌎");
}, 2000);

// setInterval(greeting, 2000, "Rasel");
